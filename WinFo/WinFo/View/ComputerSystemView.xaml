<Window x:Class="WinFo.View.ComputerSystemView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WinFo.ViewModel"
        mc:Ignorable="d"
        Title="ComputerSystemView" Height="450" Width="800">
    <Window.DataContext>
        <local:ComputerSystemViewModel/>
    </Window.DataContext>
    <Grid>
        <DataGrid IsReadOnly="True" ItemsSource="{Binding Path=ComputerSystem}" Margin="10"/>

    </Grid>
</Window>
